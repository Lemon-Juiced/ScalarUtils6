package site.scalarstudios.scalarutils.item.blockitem;

import net.minecraft.world.item.BlockItem;
import net.minecraft.world.level.block.Block;

// Used for classification in the creative tabs.
public class InvertedFlatLightBlockItem extends BlockItem {
    public InvertedFlatLightBlockItem(Block block, Properties properties) {
        super(block, properties);
    }
}
